import GoogleButton from "@/components/GoogleButton";
import React from "react";

const page = () => {
  return (
    <div className="h-screen max-container flex justify-center items-center">
      <GoogleButton />
    </div>
  );
};

export default page;
