/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "static.coinstats.app",
      "i.pinimg.com",
      "lh3.googleusercontent.com",
    ],
  },
};

module.exports = nextConfig;
